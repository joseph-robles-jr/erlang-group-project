
-module(main).
-export([test/0]).

main()->

ok.

%[1,0]
%ns,ew

%wait 10 seconds then switch [0,1]
%if button pressed cut time in half
%car or person requests states, if states allow, proceed. Only person can speed up state change.
%car or person needs to also provide the direction they are heading, north, south, east, west. 

test()->

ok.